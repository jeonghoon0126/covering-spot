📋 *릴리즈 노트 — 2026-03-02*

*🚛 배차 관리 개선*
1. 재배차 도구 임시 비활성화 — 향후 재구현 예정 (UI 메뉴에서 노출 제거)
2. 주문 목록 시간순 정렬 버그 수정 — 오전→오후→저녁 순, 동일 슬롯 내 접수 순서(createdAt)로 정렬

*🗺️ 기사별 배차 현황(Gantt) 개선*
3. 하차지 수동 컨트롤 — Gantt에서 수거지 블록 hover → + 버튼으로 하차지 추가, 하차지 badge에서 × 버튼으로 제거
4. 간트 막대 색상 기사별 변경 — 상태별 색상 → 기사별 고유 색상, 범례 업데이트
5. 드래그 드랍 즉각 반응 — Optimistic Update 적용, 드래그 후 즉시 UI 반영 (로딩 대기 제거)
6. 하차지 추가 드롭다운 개선 — 포털 방식(position:fixed)으로 다른 막대에 가려지는 문제 해결
7. 기사님 관리 버튼 개선 — 배차 관리 헤더에서 항상 노출, 기사 관리 → 배차 관리 복귀 버튼 추가

*⚙️ 인프라*
8. 배포 방식 변경 — Vercel CLI → REST API gitSource 전환, rate limit 원천 차단
9. 배포 완료/실패 Slack DM 알림 — main push 시 즉시 수신

*✅ QA 체크리스트*
• 배차 현황 → Gantt 수거지 블록 hover → + 버튼 노출 확인
• 하차지 선택 → 블록 내 badge(하차지명 + ×) 표시, DB 반영 확인
• × 버튼으로 하차지 제거 → badge 사라짐, + 버튼 재노출 확인
• 기사별 색상 범례와 막대 색상 일치 확인
• Gantt 드래그 드랍 → 즉시 UI 반영, API 완료 후 동기화 확인
• 배차 목록 시간순 정렬 확인 (오전→오후→저녁, 같은 슬롯 내 접수 순)
